<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE  mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="tour">

	<insert id="create" parameterType="hashMap">
		INSERT INTO
		admin(admin_email,admin_password,admin_name,admin_phone_num,admin_regist_day)
		VALUES(#{email},#{password},#{name},#{phone_num},now())
	</insert>
	<select id="adminLogin" parameterType="hashMap" resultType="int">
		SELECT COUNT(*)as count FROM admin WHERE admin_email = #{id} and
		admin_password = #{password}
	</select>

	<insert id="tourInsert" parameterType="TourVO">
		INSERT INTO
		tour(tour_post_sn,tour_cmmn_fax,tour_address,tour_new_address,tour_subway_info,tour_cmmn_hmpg_url,tour_cmmn_telno,tour_cmmn_bsnde,toour_bf_desc,tour_cmmn_rstde,
		tour_cmmn_use_time,tour_post_sj)
		VALUES(#{POST_SN},#{CMMN_FAX},#{ADDRESS},#{NEW_ADDRESS},#{SUBWAY_INFO},#{CMMN_HMPG_URL},#{CMMN_TELNO},#{CMMN_BSNDE},#{BF_DESC},#{CMMN_RSTDE},
		#{CMMN_USE_TIME},#{POST_SJ});

	</insert>
	<!-- <select id="select_detail" resultType="hashMap" > SELECT * FROM admin 
		WHERE admin_email = #{email} </select> <select id="passDetail" resultType="hashMap" 
		parameterType="hashMap"> SELECT * FROM admin WHERE admin_email = #{email} 
		AND admin_password = #{password} </select> <update id="saveUpdate" parameterType="hashMap"> 
		UPDATE admin SET admin_name=#{name},admin_regist_day= now() WHERE admin_email=#{email} 
		</update> <delete id="delete" parameterType="hashMap" > delete from admin 
		where trim(admin_email = #{email}) </delete> <select id="list" parameterType="hashMap" 
		resultType="AdminVO"> select * from admin <if test="search != null and search 
		!= '' "> WHERE name LIKE CONCAT('%',#{search},'%') or id LIKE CONCAT('%',#{search},'%') 
		</if> </select> <select id="select_seq" resultType="hashMap" parameterType="hashMap"> 
		SELECT admin_seq FROM admin </select> -->

</mapper>